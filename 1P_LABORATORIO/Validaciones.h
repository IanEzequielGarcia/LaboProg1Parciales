#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
int ValidarInt(char[]);
int ValidarString(char []);
char IngresarString(char []);
int IngresarInt(char []);

void ValidacionTipo(char []);
void ValidacionTamanio(char []);
